# InsightForge Backend Environment Variables
# Copy this as .env and fill with your actual values

# Optional API key to protect /analyze endpoint (leave empty for public access)
ECOM_AGENT_API_KEY=

# CORS Origins (comma-separated) - for frontend communication
# Development:
ECOM_AGENT_CORS_ORIGINS=http://localhost:5500,http://127.0.0.1:5500,http://localhost:3000

# Production (Firebase Hosting):
# ECOM_AGENT_CORS_ORIGINS=https://insightforge.web.app,https://insightforge.firebaseapp.com

# Runtime port (Render/Railway typically set this automatically)
PORT=8000

# Python output buffering (set to true for production logging)
PYTHONUNBUFFERED=true
